module.exports = function(grunt){
	grunt.initConfig({
		pkg: grunt.file.readJSON('package.json')
		// tasks
		/*min : {
			dist: {
				src: ['src/documents/moment.js'],
				dest: 'out/moment.min.js'
			}
		}*/
	});
}